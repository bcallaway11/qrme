% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-select.R
\name{tsme_model_select}
\alias{tsme_model_select}
\title{tsme_model_select}
\usage{
tsme_model_select(
  data,
  y_formula,
  t_formula,
  tau,
  t_values,
  copulas = c("gaussian", "clayton", "gumbel", "frank"),
  me_distributions = c("gaussian", "laplace"),
  y_n_mix = 1L,
  t_n_mix = 1L,
  n_cores = 1L,
  return_fits = FALSE,
  ...
)
}
\arguments{
\item{data}{data.frame passed to \code{\link{tsme}}}

\item{y_formula}{formula for the outcome model}

\item{t_formula}{formula for the treatment model}

\item{tau}{quantile levels for the first-stage QR (passed to
\code{\link{tsme}})}

\item{t_values}{treatment values for conditional distribution summaries}

\item{copulas}{character vector of copula families to try.  Any subset of
\code{c("gaussian","clayton","gumbel","frank")} (default: all four).}

\item{me_distributions}{character vector of ME distributions to try.  Any
subset of \code{c("gaussian","laplace")} (default: both).}

\item{y_n_mix}{number of ME mixture components for the outcome equation
(default 1)}

\item{t_n_mix}{number of ME mixture components for the treatment equation
(default 1)}

\item{n_cores}{number of parallel workers (default 1); set to
\code{length(copulas) * length(me_distributions)} to run one worker per
grid cell}

\item{return_fits}{logical; if \code{TRUE} the fitted tsme objects are
returned alongside the table (default \code{FALSE})}

\item{...}{additional arguments passed to \code{\link{tsme}} for every cell
in the grid (e.g. \code{n_copula_me_draws}, \code{mcmc_draws})}
}
\value{
a list with element \code{table} (a data.frame sorted by BIC) and,
  if \code{return_fits = TRUE}, element \code{fits} (a list of tsme objects
  in the same order as \code{table})
}
\description{
Fit \code{\link{tsme}} over a grid of copula families and
  measurement error distributions and rank the specifications by AIC and BIC.

  The log-likelihood is computed as the sum of three components:
  \itemize{
    \item \code{ll_y}: log-likelihood of the outcome ME model
    \item \code{ll_t}: log-likelihood of the treatment ME model
    \item \code{ll_cop}: copula log-likelihood
  }
  \deqn{AIC = -2 \ell + 2k, \quad BIC = -2 \ell + k \log n}
  where \eqn{k} is computed automatically from \code{y_n_mix} and
  \code{t_n_mix}: \eqn{k = k_Y + k_T + 1}, with \eqn{k_m = 2} for
  \eqn{m = 1} and \eqn{k_m = 3m - 1} for \eqn{m \ge 2}, plus one
  copula parameter.
}
\examples{
\dontrun{
  tau      <- seq(0.05, 0.95, length.out = 15)
  t_values <- quantile(nlsy97$lpi, probs = seq(0.1, 0.9, by = 0.1))
  sel <- tsme_model_select(
    data             = nlsy97,
    y_formula        = lci ~ ageC_97 + ageF,
    t_formula        = lpi ~ ageC_97 + ageF,
    tau              = tau,
    t_values         = t_values,
    y_n_mix          = 1L,
    t_n_mix          = 1L,
    n_cores          = 4L,
    mcmc_draws       = 200L,
    mcmc_burn_in     = 20L
  )
  print(sel)
}

}
